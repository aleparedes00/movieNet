<Application x:Class="MovieNet.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MovieNetData.ViewModel;assembly=MovieNetData"
             xmlns:movienet="clr-namespace:MovieNet"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <local:VMLocator x:Key="VMLocator"/>
            <DataTemplate DataType="{x:Type local:NewFilmVM}">
                <movienet:NewFilmView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type local:ProfileVM}">
                <movienet:ProfileView/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
