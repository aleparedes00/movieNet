<Window x:Class="MovieNet.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MovieNetData.ViewModel;assembly=MovieNetData"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource VMLocator}, Path=LoginVM}"
        Title="Login" Height="450" Width="414.447" FontSize="16" Background="CornflowerBlue">
    <Border Background="Black" CornerRadius="20" Margin="20">
        <StackPanel Margin="20">
            <Label Content="Login" Foreground="White" FontSize="25" HorizontalAlignment="Center"/>
            <Separator></Separator>
            <Label Content="Username" Foreground="White"/>
            <TextBox x:Name="txtUsername" Background="Gray" Foreground="White" FontSize="18"/>
            <Label Content="Password" Foreground="White"/>
            <PasswordBox Name="txtPassword" Background="Gray" Foreground="White" FontSize="18"/>
            <Button Name="BtnSumit" Command="{Binding LoginCommand}" Content="Sumit" Margin="70 10" Background="Gray" Foreground="White" FontSize="18"/>
        </StackPanel>
    </Border>
</Window>
